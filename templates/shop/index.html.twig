{% extends 'base.html.twig' %}

{% block title %}Vente d'occasion
{% endblock %}

{% block main %}
	<h2>Annonces d'occasion</h2>
	{# {% for car in cars %} #}
		<div class="card mb-3" style="border-color:var(--color-primary)">
			<div class="row no-gutters">
				<div class="col-md-4">
					<img src="{{asset('images/voiture_1.png')}}" class="card-img-top" alt="...">
				</div>
				<div class="col-md-8">
					<div class="card-body">
						{# <h5 class="card-title">{{ cars.model_type }}</h5> #}
						{# <p class="card-text">Légende</p> #}
						<ul>
							{# <li>{{ cars.price }}</li>
							<li>{{ cars.amc }}</li>
							<li>{{ cars.kilometres }}</li>
							<li>{{ cars.fuel }}</li>
							<li>{{ cars.BV_transmission }}</li> #}
						</ul>
					</div>
				</div>
			</div>
		</div>
	{# {% endfor %} #}
{% endblock %}
