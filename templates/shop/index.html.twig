{% extends 'base.html.twig' %}

{% block title %}Vente d'occasion
{% endblock %}

{% block main %}
	<h2>Annonces d'occasion</h2>
	{# {% for car in cars %} #}
		<div class="card mb-3" style="border-color:var(--color-primary)">
			<div class="row no-gutters">
				<div class="col-md-4">
					<img src="{{asset('images/voiture_1.png')}}" class="card-img-top" alt="...">
				</div>
				<div class="col-md-8">
					<div class="card-body">
						{# <h5 class="card-title">{{ app.cars.model_type }}</h5> #}
						<p class="card-text">Légende</p>
						<ul>
							{# <li>{{ app.cars.price }}</li> #}
							{# <li>{{ app.cars.amc }}</li> #}
							{# <li>{{ app.cars.kilometres }}</li> #}
							{# <li>{{ app.cars.fuel }}</li> #}
							{# <li>{{ app.cars.BV_transmission }}</li> #}
						</ul>
					</div>
				</div>
			</div>
		</div>
	{# {% endfor %} #}
{% endblock %}
